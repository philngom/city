export function createCountString(waterfrontCount, skylineCount, castleCount) {
    return `You clicked on waterfront ${waterfrontCount} times, on skyline ${skylineCount} times and castle ${castleCount} times.`;
}